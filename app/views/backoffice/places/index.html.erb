<p id="notice"><%= notice %></p>

<h1>Places</h1>

<table>
  <thead>
    <tr>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @places.each do |place| %>
      <tr>
        <td><%= place.description %></td>
        <td><%= link_to 'Show', backoffice_place_path(place) %></td>
        <td><%= link_to 'Edit', edit_backoffice_place_path(place) %></td>
        <td><%= link_to 'Destroy', backoffice_place_path(place), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Place', new_backoffice_place_path %>
